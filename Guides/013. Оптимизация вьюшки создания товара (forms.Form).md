# Оптимизация вьюшки создания товара (forms.Form)

1. Внести изменения в файл «store/views.py»:  
	1) Сразу после строки `def create_product(request):` добавить строку:  
    ```python 
    form = ProductForm(request.POST or None)
	``` 

	2) После строки `if request.method == "POST":` удалить строку:  
    ```python 
    form = ProductForm(request.POST)
    ``` 

	3) Удалить строки:  
    ```python 
        else:
        return render(request, 'store/product_add.html', {"form": form})
    
    # GET
    form = ProductForm()

	``` 

	4) Вместо строк, которые удалили, записать строку:  
	```python 
    # Если форма невалидна, продолжим к render ниже.
    ``` 

2) (Опционально.) Создать коммит: «refactor(products): Оптимизировал вьюшку создания товара (forms.Form).»
