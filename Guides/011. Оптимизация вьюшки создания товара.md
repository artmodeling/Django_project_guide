# Оптимизация вьюшки создания товара

1. Внести изменения в файл «store/views.py»:  
	1) Сразу после строки `def create_product(request)` стоят две строки, которые нужно удалить вместе с пустой строкой после них:  
	```python
    if request.method == "GET":
        return render(request, 'store/product_add.html')


    ``` 

2) После строки `return render(request, 'store/product_add.html', context)`:  
\- добавить одну пустую строку,  
\-  и еще одну строку с кодом, начало которой по отступам слева должно быть на уровне циклов «if», которые находятся ближе всех «if» к левому краю:  
`return render(request, 'store/product_add.html')`  

2. Проверить на сайте, что страница html, для которой вносили изменения, работает.

3. (Опционально.) Создать коммит: «refactor(products): Оптимизировал вьюшку создания товара.»
